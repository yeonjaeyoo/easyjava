특정 스트링을 사용하는 알고리즘을 => 특정 알고리즘의 스트링을 

해시에 => 해쉬에서

풋(put) => put

Page 126.

다음 표에 나와있는 목록을 보면 => 다음 표와 같이

DHT의 사용 ~ 한다. => JDHT를 사용하여 DHT의 사용을 설명한다.

다음 표에 나와있는 => 다음 표에서 나타내는

사용되는 메인 jar 파일이다 => 메인 jar 파일로 사용된다.

다른 두 개의 ~ 사용된다. => JDHT는 다른 두 개의 JAR 파일을 사용한다.

다음과 같이 ~ 나열된다. => 확인필요

하나의 => X

포트 4440을 => 4440 포트를

이 인스턴스로 => JDHT 인스턴스로

put 메소드을 => put 메소드를

Page 127.

