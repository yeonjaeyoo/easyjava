Page 8.

다음에 => 이 후

자바 EE 에디션 => 자바 EE 에디션(Java EE Edition)

그것을 => X

대화 상자 => 확인

자바 웹 => 자바 웹(Java Web)

웹 애프리케이션 => 웹 애플리케이션(Web Application)

다음 버튼 => 다음(Next) 버튼

Page 9.

Next => 다음(Next)

대화 상자 => 확인

배치될 위치이다. => 위치할 경로이다.

Java => 자바

Simple ~ 중복이다. => 확인

그 목적은 => 목적은

메소드와 => 메소드로

생성 된 return 문은 => 생성된 return 구문은

중간 단계 모두는 => 모든 중간 단계는

리소스에 접속할 수 있는 => 리소스에 접속하는

창에서 => 윈도우에서(?)

Page 10.

표시한다. => 표시할 것이다.

테스트를 위한 메소드를 => 테스트 메소드를

다음과 같이 => 이 후 다음과 같이

그러나 => 그러나 이것은

여기 => 여기에서

이 서비스 => RESTful 서비스

웹 서비스 범주에서 => 웹 서비스(Web Services) 카테고리에서

대화 상자가 나타난다. => 대화 상자가 나타날 것이다.

나타난다. => 나타날 것이다.

확장하고 => 확장하여

완료 대화 상자 => ?

Page 11.
 
이 후 => 이 후에

다음과 같다. => 다음과 같을 것이다.

콘솔에 => 콘솔에서

RESTful 서비스에서 => 이것은 RESTful 서비스에서

방법을 => 과정을


                        