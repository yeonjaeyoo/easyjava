Page 115.

ifModifiedSince 변수는 오브젝트 검색의 발생여부에 따라 제어한다. ifModifiedSince 변수는 특정시점(1970년 1월 1일 GMT) 이후의 밀리초로 시간을 나타내는 long 값이다. 오브젝트가 그 시간보다 좀 더 최근에 수정된 경우, 패치(fetch) 된다.

다음 표는 ~ 요약했다. 

표 : setDoInput -> setDoOutput 변경

Tomcat 같은 보다 정교한 서버는 구성방법을 제어하는 더 많은 옵션이 있다.

-----------------------------------------
sendGet 메소드는  ~ 따른다.=>수정해야함

그러나 유사한 ~ 사용할 수 있다.=> 그러나(X) 사용할 수도 있다.

명확하게 -> 명확한

예를 들면, 단지 GET 메소드만 사용하는 것이다.=>확인

다음에 보여준다. => 다음과 같이 보여준다.

이 연결을 => 이 connection을
요청 방법 => 요청 메소드

응답 코드를 검색하고, 성공하면 => 응답 코드를 검색하여, 성공이면

입력 스트림을 반환한다=>입력 스트림을 반환한다.

StringBuilder 인스턴스는 생성하기 위해 => StringBuilder 인스턴스는

URL이 형성되는 경우 => URL이 생성되면

일부는 예약되고 이 외는 예약되지 않은 것이다. => 일부는 예약문자이고 그 외는 예약되지 않은 문자이다.

2개 인자 메소드는 => 2개의 인자를 사용한 메소드는

캐릭터 인코딩 방식 => 문자 인코딩 방식

다음 코드가 => 다음 코드는

http~sd => http~sdk

이전 서버와 => 이전에 생성한 서버와

클라이언트 ~ 부른다. => 확인해야함

이것과 => HTTPServer 클래스, 

다음 표와 같다. => 다음과 같다.

